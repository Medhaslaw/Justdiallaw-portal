import { Theme } from './symbols';

export const lightTheme: Theme = {
  name: 'light',
  properties: {
    '--background': '#0b0b31',
    '--on-background': '#000',
    '--primary': '#e86d1f',
    '--on-primary': '#000'
  }
};
